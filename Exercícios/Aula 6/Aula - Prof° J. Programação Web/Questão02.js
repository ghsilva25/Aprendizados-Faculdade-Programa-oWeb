for (let x = 1; x<=6; x++) {
    let sorteio = Math.floor(Math.random() *100)

    if(sorteio<10) {
        console.log(` 0${ sorteio } `) }
    
        else{
            console.log( ` ${ sorteio } ` )
        }
    
    
    }

    
